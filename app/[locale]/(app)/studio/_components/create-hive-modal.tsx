'use client'

// TODO H1-T12: replace this stub with the real CreateHiveModal.
// For now it accepts open/onOpenChange and renders nothing so the
// Hives section can wire its trigger state without runtime errors.

type Props = {
  open: boolean
  onOpenChange: (open: boolean) => void
  // Optional seed values future T12 may consume.
  defaultBookId?: string | null
}

export function CreateHiveModal(_props: Props) {
  return null
}
