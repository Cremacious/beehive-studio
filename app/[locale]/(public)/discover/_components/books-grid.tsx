type Props = {
  sp: Record<string, string | string[] | undefined>
  locale: string
}

export function BooksGrid(_props: Props) {
  return (
    <div className="text-[14px] text-[var(--canvas-dark-ink-muted)] py-8">
      TODO: BooksGrid (wires in W4.2 after action extension)
    </div>
  )
}
