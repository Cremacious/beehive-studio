// W3.5 of discover redesign. v1 ships as a thin re-export of the existing
// RailClubCard — already renders the club cell with current_book cover,
// member avatar stack, OPEN pill when openJoin. Grid layout uses
// justify-items-start to accommodate the card's fixed width.
export { RailClubCard as ClubGridCard } from './rail-club-card'
