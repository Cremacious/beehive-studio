// W3.3 of discover redesign. v1 ships as a thin re-export of the existing
// RailHiveCard — already renders the hive cell with linked-book thumb,
// member avatar stack, activity pulse. Grid layout uses justify-items-start
// to accommodate the card's fixed width.
export { RailHiveCard as HiveGridCard } from './rail-hive-card'
