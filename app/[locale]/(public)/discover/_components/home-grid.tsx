type Props = {
  sp: Record<string, string | string[] | undefined>
  locale: string
}

export function HomeGrid(_props: Props) {
  return (
    <div className="text-[14px] text-[var(--canvas-dark-ink-muted)] py-8">
      TODO: HomeGrid (wires in W6 — interleaved cross-entity grid)
    </div>
  )
}
